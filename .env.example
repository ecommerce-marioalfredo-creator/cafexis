# URL base del backend de Cafexis (aún no existe). Cuando esté disponible,
# define esta variable en un archivo .env local para que services/api/client.ts
# deje de depender del mock y llame a la API real.
VITE_API_BASE_URL=http://localhost:3000/api
